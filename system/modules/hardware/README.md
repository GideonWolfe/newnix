These modules exist to support various bits of optional hardware a given host might have.

- Hard Drives
- Bluetooth
- Gamepads
- etc.

Most of these modules are included in the `hardware` role but can be imported indivudually by a host if desired.